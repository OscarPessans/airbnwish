<%= simple_form_for [flat, booking] do |f| %>
  <%= f.input :start_date, label: "Arrivée" %>
  <%= f.input :end_date, label: "Départ" %>
  <%= f.submit "Réservez !!!", class: "btn btn-primary"%>
<% end %>
